{
  "name": "@auren/workspace",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.21.0",
  "engines": {
    "node": ">=20.19.0 <26",
    "pnpm": "11.21.0"
  },
  "scripts": {
    "check": "pnpm --filter @auren/schemas build && node scripts/verify-workspace.mjs && node scripts/verify-blocks.mjs",
    "build": "turbo run build",
    "dev": "turbo run dev",
    "test": "pnpm --filter @auren/schemas build && node --test scripts/verify-blocks.test.mjs && turbo run test",
    "typecheck": "turbo run typecheck",
    "lint": "biome lint .",
    "lint:fix": "biome lint --write .",
    "format": "biome format .",
    "format:fix": "biome format --write ."
  },
  "devDependencies": {
    "@auren/schemas": "workspace:*",
    "@biomejs/biome": "2.5.10",
    "turbo": "2.10.12",
    "typescript": "7.0.2",
    "vitest": "4.1.11"
  }
}
